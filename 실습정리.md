# Django 블로그 실습 정리 (코드 중심)

웹서비스프로그래밍 실습 5회차 동안 작성/수정한 코드 정리.

| 회차 | 주제 | 핵심 커밋 |
|------|------|-----------|
| 실습 1 | 프로젝트 & 모델 생성 | `0707fc1` |
| 실습 2 | 뷰 · URL · 템플릿 연결 | `75dfb4f` |
| 실습 3 | HTML / CSS 페이지 | `6d3935a`, `e44d0b5` |
| 실습 4 | 템플릿 상속 + 폼 CRUD | `97c05d6`, `6b087ad` |
| 실습 5 | 이미지 업로드 + JavaScript | `54be831`, `8f75317` |

---

## 실습 1 — Django 프로젝트 & 블로그 앱 생성

> 프로젝트(`mysite`) / 앱(`blog`) 생성, 글(Post) 데이터 모델 설계

### `blog/models.py`
```python
from django.db import models
from django.utils import timezone


class Post(models.Model):
    author = models.ForeignKey('auth.User', on_delete=models.CASCADE)
    title = models.CharField(max_length=200)
    text = models.TextField()
    created_date = models.DateTimeField(default=timezone.now)
    published_date = models.DateTimeField(blank=True, null=True)

    def publish(self):
        self.published_date = timezone.now()
        self.save()

    def __str__(self):
        return self.title
```

- `blog/migrations/0001_initial.py` 생성 → DB 테이블 생성
- `manage.py`, `mysite/settings.py`, `mysite/urls.py`, `blog/admin.py` 등 기본 골격 생성
- `db.sqlite3` 생성

---

## 실습 2 — 뷰 · URL · 템플릿 연결 (MTV 흐름)

> 요청 → 뷰 → 템플릿 응답으로 이어지는 Django 기본 흐름 연결

### `blog/views.py`
```python
from django.shortcuts import render


def post_list(request):
    return render(request, 'blog/post_list.html', {})
```

### `blog/urls.py` (신규)
```python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.post_list, name='post_list'),
]
```

### `mysite/urls.py`
```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('blog.urls')),   # 앱 URL 포함
]
```

### `mysite/settings.py`
```python
# 배포(pythonanywhere)용 호스트 허용
ALLOWED_HOSTS = ['127.0.0.1', 'pythonanywhere.com', 'pataegwon.pythonanywhere.com']
```

- `blog/templates/blog/post_list.html` : 첫 정적 HTML ("KHU's blog" + 더미 글)

---

## 실습 3 — HTML / CSS 페이지 제작

> 템플릿을 순수 HTML/CSS로 직접 꾸미기 (윤동주 「자화상」 시 페이지)

### `blog/templates/blog/post_list.html` (전면 교체)
```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>자화상 - 윤동주</title>
    <style>
        body { margin: 0; color: #222; font-family: "Malgun Gothic", sans-serif; }
        .page { width: calc(100% - 40px); margin: 20px auto 0; }
        header { padding-bottom: 20px; border-bottom: 10px solid #858585; }
        .poem-title {
            width: 50%; margin: 0 auto 38px; padding-bottom: 20px;
            border-bottom: 1px solid #cfcfcf; text-align: center;
        }
        .poem p { margin: 0 0 14px; line-height: 1.45; }
        aside ul { margin: 0; padding-left: 40px; line-height: 1.55; }
        footer { margin-top: 12px; padding: 12px 0; border-top: 1px solid #cfcfcf; }
    </style>
</head>
<body>
    <div class="page">
        <header><h1>자화상 - 윤동주</h1></header>

        <section>
            <div class="poem-title"><h2>&lt; 자화상 &gt;</h2><p>윤동주</p></div>
            <div class="poem">
                <p>산모퉁이를 돌아 논가 외딴 우물을 홀로 찾아가선 가만히 들여다봅니다.</p>
                <!-- ... 시 본문 ... -->
            </div>
        </section>

        <aside>
            <h2>윤동주 시인의 다른 시</h2>
            <ul><li>서시</li><li>자화상</li><li>별 헤는 밤</li><!-- ... --></ul>
        </aside>

        <footer>&copy;웹서비스프로그래밍 @2026</footer>  <!-- @2020 → @2026 수정 -->
    </div>
</body>
</html>
```

**포인트:** 시맨틱 태그(`header`/`section`/`aside`/`footer`) + 내부 `<style>` CSS

---

## 실습 4 — 동적 블로그 (템플릿 상속 + 폼 CRUD)

> DB 글 목록 출력 + 글 작성/수정/상세보기 구현, 템플릿 상속으로 중복 제거

### `blog/views.py` (뷰 4개로 확장)
```python
from django.shortcuts import render, get_object_or_404, redirect
from django.utils import timezone
from .models import Post
from .forms import PostForm


def post_list(request):
    posts = Post.objects.filter(published_date__lte=timezone.now()).order_by('published_date')
    return render(request, 'blog/post_list.html', {'posts': posts})


def post_detail(request, pk):
    post = get_object_or_404(Post, pk=pk)
    return render(request, 'blog/post_detail.html', {'post': post})


def post_new(request):
    if request.method == "POST":
        form = PostForm(request.POST)
        if form.is_valid():
            post = form.save(commit=False)
            post.author = request.user
            post.published_date = timezone.now()
            post.save()
            return redirect('post_detail', pk=post.pk)
    else:
        form = PostForm()
    return render(request, 'blog/post_edit.html', {'form': form})


def post_edit(request, pk):
    post = get_object_or_404(Post, pk=pk)
    if request.method == "POST":
        form = PostForm(request.POST, instance=post)   # instance=수정 대상
        if form.is_valid():
            post = form.save(commit=False)
            post.author = request.user
            post.published_date = timezone.now()
            post.save()
            return redirect('post_detail', pk=post.pk)
    else:
        form = PostForm(instance=post)
    return render(request, 'blog/post_edit.html', {'form': form})
```

### `blog/forms.py` (신규 — ModelForm)
```python
from django import forms
from .models import Post


class PostForm(forms.ModelForm):
    class Meta:
        model = Post
        fields = ('title', 'text',)
```

### `blog/urls.py` (경로 추가)
```python
urlpatterns = [
    path('', views.post_list, name='post_list'),
    path('post/<int:pk>/', views.post_detail, name='post_detail'),
    path('post/new/', views.post_new, name='post_new'),
    path('post/<int:pk>/edit/', views.post_edit, name='post_edit'),
]
```

### `blog/templates/blog/base.html` (신규 — 공통 틀)
```html
{% load static %}
<!DOCTYPE html>
<html>
    <head>
        <title>Web Service programming blog</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.4.1/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.4.1/dist/css/bootstrap-theme.min.css">
        <link href='https://fonts.googleapis.com/css?family=Lobster&subset=latin,latin-ext' rel='stylesheet'>
        <link rel="stylesheet" href="{% static 'css/blog.css' %}">
    </head>
    <body>
        <div class="page-header">
            {% if user.is_authenticated %}
                <a href="{% url 'post_new' %}" class="top-menu"><span class="glyphicon glyphicon-plus"></span></a>
            {% endif %}
            <h1><a href="/">Web Service programming blog</a></h1>
        </div>
        <div class="content container">
            <div class="row"><div class="col-md-8">
                {% block content %}{% endblock %}
            </div></div>
        </div>
    </body>
</html>
```
> Bootstrap CDN을 `maxcdn` → `https://cdn.jsdelivr.net/npm/bootstrap@3.4.1` 로 교체 (깨진 링크 수정)

### `blog/templates/blog/post_list.html` (상속 구조로 변경)
```html
{% extends 'blog/base.html' %}

{% block content %}
    {% for post in posts %}
        <div class="post">
            <div class="date">{{ post.published_date }}</div>
            <h1><a href="{% url 'post_detail' pk=post.pk %}">{{ post.title }}</a></h1>
            <p>{{ post.text|linebreaksbr }}</p>
        </div>
    {% endfor %}
{% endblock %}
```

### `blog/templates/blog/post_detail.html` (신규)
```html
{% extends 'blog/base.html' %}

{% block content %}
    <div class="post">
        {% if post.published_date %}
            <div class="date">{{ post.published_date }}</div>
        {% endif %}
        {% if user.is_authenticated %}
            <a class="btn btn-default" href="{% url 'post_edit' pk=post.pk %}"><span class="glyphicon glyphicon-pencil"></span></a>
        {% endif %}
        <h1>{{ post.title }}</h1>
        <p>{{ post.text|linebreaksbr }}</p>
    </div>
{% endblock %}
```

### `blog/templates/blog/post_edit.html` (신규)
```html
{% extends 'blog/base.html' %}

{% block content %}
    <h1>New post</h1>
    <form method="POST" class="post-form">{% csrf_token %}
        {{ form.as_p }}
        <button type="submit" class="save btn btn-default">Save</button>
    </form>
{% endblock %}
```

### `blog/static/css/blog.css` (신규 — 주요 스타일)
```css
.page-header { background-color: #ff9400; padding: 20px 20px 20px 40px; }
.page-header h1 a { color: #fff; font-size: 36pt; text-decoration: none; }
.content { margin-left: 40px; }
h1, h2, h3, h4 { font-family: 'Lobster', cursive; }
.date { color: #828282; }
.post { margin-bottom: 70px; }
.top-menu { color: #fff; float: right; font-size: 26pt; margin-right: 20px; }
```

---

## 실습 5 — 이미지 업로드 + JavaScript

> 글에 이미지 첨부(미디어 파일) + JavaScript 숫자 맞히기 게임

### 1) 이미지 업로드

#### `blog/models.py` (image 필드 추가)
```python
image = models.ImageField(
    upload_to='intruder_image/%Y/%m/%d/',
    default='intruder_image/default_error.png',
)
```
- `blog/migrations/0002_post_image.py` 생성

#### `mysite/settings.py` (미디어 설정)
```python
STATIC_URL = '/static/'                      # 절대 경로로 수정
STATIC_ROOT = os.path.join(BASE_DIR, 'static')

MEDIA_URL = "/media/"
MEDIA_ROOT = BASE_DIR / "media/"
```

#### `mysite/urls.py` (정적/미디어 서빙)
```python
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('blog.urls')),
]
urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

#### `blog/templates/blog/post_list.html` (이미지 출력 추가)
```html
<!-- intruder image -->
<img src="{{ post.image.url }}" class="rounded intruder-image-list" alt="{{ post.title }}">
```

#### `blog/static/css/blog.css` (추가)
```css
.intruder-image-list { object-fit: cover; }
```

### 2) JavaScript — 숫자 맞히기 게임

#### `blog/templates/blog/js_test.html` (신규) — 버그 3개 수정이 핵심
```javascript
// 버그1: *100 누락 → 항상 1만 나옴
let randomNumber = Math.floor(Math.random() * 100) + 1;

// 버그2: 클래스 선택자에 점(.) 누락 ('lowOrHi' → '.lowOrHi')
const lowOrHi = document.querySelector('.lowOrHi');

// 버그3: addeventListener → addEventListener (대소문자)
guessSubmit.addEventListener('click', checkGuess);
resetButton.addEventListener('click', resetGame);

function resetGame() {
    // ...
    randomNumber = Math.floor(Math.random() * 100) + 1;  // 새 게임마다 재설정
}
```

#### `blog/views.py` / `blog/urls.py`
```python
# views.py
def js_test(request):
    return render(request, 'blog/js_test.html', {})

# urls.py
path('js_test/', views.js_test, name='js_test'),
```
